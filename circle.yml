machine:
  ruby:
    version: 2.3.1
  services:
    - redis
general:
  artifacts:
    - "tmp/capybara"
dependencies:
  post:
    - bundle exec rake rubocop
    - gem install derby
    - derby-ldp:
        background: true
    - bundle exec rake server:test:
        background: true
    - bin/jetty_wait
