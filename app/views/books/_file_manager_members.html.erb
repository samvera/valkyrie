<ul id="sortable" data-id="<%= CGI.escape(@record.id) %>" data-class-name="<%= @record.model_name.plural %>" data-singular-class-name="<%= @record.model_name.singular %>" class="list-unstyled grid clearfix">
 <% @children.each do |member| %>
   <%= render "file_manager_member", node: member %>
 <% end %>
</ul>
